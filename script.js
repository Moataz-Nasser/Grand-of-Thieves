const toggleBtns=document.querySelectorAll(".menu-toggle");const menu=document.getElementById("menuPhone");const menuContainer=document.getElementById("menuPhoneContainer");toggleBtns.forEach(b=>b.addEventListener("click",()=>{menu.classList.toggle("active")}));document.addEventListener("click",(e)=>{const menuActive=menu.classList.contains("active");if(menuActive&&!menuContainer.contains(e.target)&&!Array.from(toggleBtns).some(btn=>btn.contains(e.target))){menu.classList.remove("active")}});const nav=document.querySelector("header");const navHeight=nav.clientHeight;window.addEventListener("scroll",()=>{if(window.scrollY>navHeight+50){nav.classList.add("fixed-header")}else{nav.classList.remove("fixed-header")}});document.addEventListener("DOMContentLoaded",()=>{const paddedBottom=document.querySelector(".padded-bottom");const footer=document.querySelector('footer');const footerHeight=footer.clientHeight;paddedBottom.style.paddingBottom=footerHeight+"px"})